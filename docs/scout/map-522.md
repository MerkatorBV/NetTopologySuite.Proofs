# Map — #522 RelateNG honest classifier (bar 1 → bar 2)

A wayfinder map. Tickets already live as GitHub sub-issues of
[#522](https://github.com/grootstebozewolf/NetTopologySuite.Proofs/issues/522)
(`522-a` … `522-l`). This file is the **living frontier**, because the
ticket table in the epic comment is the design of record and is no longer
the live gate. Charted 2026-08-29.

> **Tracker.** The children stay on GitHub. This map does not mint claimIds
> and does not replace the epic. Official unused leftover letter if a later
> session needs one: **`522-n`**. Do not remint ADR-0004. Do not steal
> `522-e`, `522-h`, `522-k`, `522-l` for a different ask.

## Destination

**Bar level 2 across every wired triangle regime**, after each regime has
climbed through bar 1. A decline stays a decline (`UNSUPPORTED` on the
oracle wire; `im_unsupported` in Coq), never a confident `FFFFFFFFF`.

Bar 1 = an emitted regime is true geometry against the specified interior
(`0 < gtri`, ADR-0003). Bar 2 = each of the nine cells is the true DE-9IM
value for that geometry.

The epic stays open until #578 posts the closing summary and the owner
signs off.

## Notes

**Item 1 of the original ask is done.** PR #530 replaced the fallthrough
with `im_unsupported`. Overlapping triangles no longer compute as disjoint.

**Bar 1 is wired for the named regimes.** Overlap #570 / `522-b`, disjoint
#571 / `522-c`, vertex-touch #572 / `522-i`, contains via the #567
predicates plus the `contains_b` bridge. Completeness is **false**: the
T-junction and obtuse-at-v pairs still emit `TPR_Unsupported` (#577 /
`522-j`, leftover `522-m`). Five names are not a partition. Do not invent
those certificates unless asked; `522-n` is the leftover letter.

**Bar 2 has a beachhead, not a remint.** #573 / `522-d` proves the nine
gtri cells of OGC FF2FF1212 on the #571 sentinel, and the Qex that
`DE9IM.v` `pat_disjoint` rejects that fill. The classifier still emits
`aa_matrix_disjoint` = FFFFFFFFF. Reminting that shared pin is later, **not**
#575 / `522-f`.

**Oracle token is a draft, not main.** #575 / `522-f` PR #588 (draft): Coq
Qed + Qex and a thin oracle catalog. The ticket witness "#530 declined pair
returns UNSUPPORTED" is **false** — that pair is disjoint. The honest
decline vector is the T-junction. Harness leftover
(`GeosOracleBugHunt` / `CurveOracleBugHunt` token allowlist) still sits on
#575. Do not treat a merged-looking Notion row as "ticket done".

**Frozen anchors.** `touch_int_ext_exclusion`,
`touch_triangle_ii_separation_not_unconditional`,
`triangles_touch_on_shared_edge`. Ray parity enters only via ADR-0003.

**Method.** Read → Green (Qed, documented failure, or counterexample) →
Refactor. Maintainable > reliable > fast. Cite theorems by name.

## Ticket table (live, 2026-08-29)

Map order is the epic comment's order. "Takeable" means every listed
blocker is closed and nobody is assigned.

| Ticket | claimId | Horizon | Blockers | State | Takeable? |
|---|---|---|---|---|---|
| #567 Regime predicates | `522-a` | Immediate | — | **open** (DoD met; leftover named) | close-or-carve, not new predicates |
| #568 Ring-inclusion groundwork | `522-g` | Expectant | — | done via #585 | — |
| #569 Carve Scholar Sam's facts | `522-k` | Immediate | — | open | **yes — first original-scope grab** |
| #570 Overlap at bar 1 | `522-b` | Immediate | #567 | done via #580 | — |
| #571 Disjoint at bar 1 | `522-c` | Immediate | #567 | done via #581 | — |
| #572 Vertex-touch at bar 1 | `522-i` | Non-urgent | #567 | done via #582 | — |
| #573 Bar 2 beachhead | `522-d` | Expectant | #571 #568 | done via #587 | — |
| #574 Prepared evaluate consults cache | `522-e` | Expectant | #571 | open | **yes** (proof) |
| #575 Oracle UNSUPPORTED token | `522-f` | Expectant | #570 #571 | open | leftover harness; draft #588 |
| #576 Bar 2 contains / touch / overlap | `522-h` | Expectant | #573 #568 | open | **yes** (proof; ticket expects a split) |
| #577 Classifier completeness | `522-j` | Non-urgent | #570 #571 #572 | done via #583; finding: completeness is false | — |
| #578 Wrap-up | `522-l` | Expectant | #574 #575 #576 #577 #569 | open | no — four blockers still open |

Unofficial leftover (no child issue): `522-m` filtered CCW-completeness,
merged as #584. Still false (obtuse-at-v).

## Decisions so far

- Item 1 / honesty sentinel — #530.
- `522-a` predicates + exclusivity — #579; `contains_b` → closed containment
  — #586. Vacuity witnesses flipped. Named leftover on #567:
  `TPR_TouchEdge` exclusivity vs the four gtri predicates. Original ticket
  text kept the shared-edge definition frozen, so that leftover is a carve,
  not a hole in the original DoD.
- Bar 1 overlap / disjoint / vertex-touch — #580 #581 #582.
- Completeness false (T-junction) — #583 / `522-j`. Retry still false
  (obtuse-at-v) — #584 / `522-m`.
- Ring-inclusion disks / dim-1 — #585 / `522-g`.
- OGC FF2FF1212 gtri cells + Qex `~ im_disjoint` — #587 / `522-d`.
  Classifier fill stays FFFFFFFFF.
- Wire token `UNSUPPORTED` (Qed + Qex that #530 is not the decline vector)
  — draft #588 / `522-f`. Not on `main` as of this chart.

## Fog

- **#567 still open.** First in map order, but the four `Prop := True`
  placeholders are gone. Owner call: retire the issue on the landed DoD, or
  keep it as the home of TouchEdge exclusivity. Do not re-do the predicates.
- **Disjoint fill remint** (FFFFFFFFF → FF2FF1212) is unnamed. Not `522-f`.
  If minted, use `522-n` or a new child of #522 — do not steal `522-e`/`h`/`k`/`l`.
- **T-junction / obtuse certificates** are unnamed. Completeness tickets
  recorded the findings and stopped. Same leftover letter rule.
- **#575 ticket witness is stale.** Drive the T-junction for `UNSUPPORTED`;
  drive #530 as classified disjoint. Harness half is the remaining DoD.
- **Empty/empty relate** stays parked (epic out-of-scope).
- **#523** (`F` vs not-computed on `CURVE_RELATE_MATRIX`) is a sibling of
  this epic, not a child. Ticket 11 of the #64–#69 map still waits on it
  for a second pass at #67.
- No `wayfinder:map` label on #522; the epic comment *is* the map. This
  file is the freshness layer.

## Frontier

Nominal first-in-order open unblocked child: **#567 / `522-a`**. That is a
paperwork decision, not a predicate session.

**First original-scope grab: #569 / `522-k`** — Immediate, unblocked,
unassigned. Carve Scholar Sam's facts out of `docs/issue-67-relateng-triage.md`,
land them on gated or living surfaces, archive the doc under `docs/history/`
with a tombstone. Docs, not Coq.

Proof grabs now unblocked, both Expectant:

- **#574 / `522-e`** — cache-consulting `evaluate`; retire
  `evaluate_ignores_cache` in the same change that makes it fail to compile.
- **#576 / `522-h`** — bar 2 for contains, touch-edge, overlap. Ticket says
  split by regime if the file grows.

**#575 / `522-f`** is mid-flight (draft #588). Next slice there is the
harness token allowlist, not more Coq, and not the disjoint remint.

**#578 / `522-l`** waits. Next useful session after this chart is
`/implement` on one takeable child, not another wayfinder pass, unless a
finding reopens fog.

```
#530 honesty ════════════════════════════════════ done

#567 522-a ── predicates + bridge ── leftover TouchEdge exclusivity
#568 522-g ── disks / dim-1 ────────────────────────────── closed
#569 522-k ── triage-doc carve ────────────────────────── FRONTIER (docs)
#570 522-b ── overlap bar 1 ────────────────────────────── closed
#571 522-c ── disjoint bar 1 ──┬── #574 522-e cache ──── FRONTIER (proof)
                               └── #575 522-f token ──── draft #588 + harness
#572 522-i ── vertex-touch bar 1 ───────────────────────── closed
#573 522-d ── FF2FF1212 cells ── #576 522-h bar 2 rest ── FRONTIER (proof)
#577 522-j ── completeness false ── 522-m still false ── closed (finding)
#578 522-l ── wrap-up ── blocked by 569, 574, 575, 576
```
