# #522 status 2026-08-31 — leftover Ⅷ classified; completeness still false

Living design of record after wrap-up and leftover `Ⅷ`. Paste onto
[#522](https://github.com/grootstebozewolf/NetTopologySuite.Proofs/issues/522)
when the tracker accepts a write; `cursor[bot]` cannot add comments or
edit the issue body. The 2026-08-29 spec comment stays the original
ticket map (`522-a` … `522-l`). The original issue body stays the
2026-08 finding. This epic remains open for owner sign-off.

topics: relate
claimId: none

## Done (do not remint)

- **Item 1 / honesty.** `relate` declines with `im_unsupported` / wire
  `UNSUPPORTED`, never a confident `FFFFFFFFF`
  (`RelateNGCore.v : relate_unsupported_no_predicate`). PR #530.
- **Wired bar 1.** Overlap / disjoint / vertex-touch / contains
  (`RelateNGOverlap.v : triangle_pair_regime_overlap`,
  `RelateNGDisjoint.v : triangle_pair_regime_disjoint`,
  `RelateNGTouchVertexRegime.v : triangle_pair_regime_touchvertex`,
  `RelateNGContainsBridge.v : contains_b_ccw_implies_closed_containment`).
- **Bar 2 gtri cells** on those regimes (disjoint FF2FF1212, contains
  212FF1FF2, touch-edge FF2F11212, overlap 212101212). Classifier fills
  stay the shared pins. Not reminted.
- **Prepared evaluate** consults the cache
  (`RelatePrepared.v : prepared_evaluate_cache_short_circuit`).
- **Every child closed.** Wrap-up is #596 / `522-l`. Closing summary:
  [`522-closing-summary.md`](522-closing-summary.md).

## Completeness

Still **false**. Live cex is an unnamed same-side shared-edge pair
A = `(0,0)(4,0)(0,4)` vs B = `(0,0)(4,0)(1,1)`
(`RelateNGNestCex.v : unnamed_ccw_pair_unsupported`).
Stop is QED ∨ QEX (`RelateNGTouchInside.v : triangle_pair_regime_ccw_stop`).
`522-j` / `522-m` keep those claimIds; do not steal them. Do not mint
`522-n`. Do not mint leftover `Ⅸ`.

## Later (leftover `Ⅸ`)

Leftover `Ⅸ` classified the nest pair that this letter named as the
live cex (`RelateNGTouchNest.v : triangle_pair_regime_nest`). The
live cex is now the swap A=`(0,0)(4,0)(1,1)` vs B=`(0,0)(4,0)(0,4)`
(`RelateNGNestCex.v : unnamed_ccw_pair_unsupported`). Do not mint
leftover `Ⅹ`. This dated letter stays leftover-`Ⅷ`-era prose; the
leftovers chart is living.

## Later (leftover `Ⅹ` / `522-n`)

Leftover `Ⅹ` classified the swap that leftover `Ⅸ` named as the
live cex (`RelateNGTouchSwapNest.v : triangle_pair_regime_swapnest`).
Board claimId is `522-n` (owner override of ADR-0004 for this letter
only). The live cex is now the identical pair A=B=`(0,0)(4,0)(0,4)`
(`RelateNGNestCex.v : unnamed_ccw_pair_unsupported`). Do not mint
leftover `Ⅺ`. This dated letter stays leftover-`Ⅷ`-era prose; the
leftovers chart is living.

## Named leftovers (Roman numerals)

Chart: [`map-522-leftovers.md`](map-522-leftovers.md). Fills stay
`im_unsupported`.

| Id | What | Headline |
|---|---|---|
| `Ⅰ` | Mutual vertex-in-open-edge sliver | `RelateNGTouchPartialEdge.v : triangle_pair_regime_touchpartial` |
| `Ⅱ` | Obtuse-at-v | `RelateNGTouchObtuse.v : triangle_pair_regime_obtuse` |
| `Ⅲ∨Ⅳ` | Exterior-side one-sided T / interior-side stem | `RelateNGTouchOnesided.v : triangle_pair_regime_onesided` / `RelateNGTouchOnesided.v : triangle_pair_regime_interior_side` |
| `Ⅴ` | Mixed-cone | `RelateNGTouchMixedCone.v : triangle_pair_regime_mixedcone` |
| `Ⅵ` | Same-cone | `RelateNGTouchSameCone.v : triangle_pair_regime_samecone` |
| `Ⅶ` | Lens | `RelateNGTouchLens.v : triangle_pair_regime_lens` |
| `Ⅷ` | Inside A=`(1,1)(2,1)(1,2)` vs B=`(0,0)(4,0)(0,4)` | `RelateNGTouchInside.v : triangle_pair_regime_inside` |

Leftover `Ⅷ` is not a remint of `contains_b` / `aa_matrix_contains`.
Do not emit `2FFFFFFF2` on that fill.

## Frozen anchors

`touch_int_ext_exclusion`,
`touch_triangle_ii_separation_not_unconditional`,
`triangles_touch_on_shared_edge`. Ray parity enters only via ADR-0003.

## Out of scope (unchanged)

Empty/empty `relate` (parked). Full noding and Touches-vs-Share (#67).
Nine-cell `geom_de9im_pointset`. Sibling #523. Classifier fill remints.
`TPR_TouchEdge` exclusivity (carved #597, not proved).

## Surfaces

TRIAGE `#522` row · [`map-522.md`](map-522.md) ·
[`map-522-leftovers.md`](map-522-leftovers.md) ·
[`522-closing-summary.md`](522-closing-summary.md) ·
[`docs/relate-ng-status.md`](../relate-ng-status.md).
