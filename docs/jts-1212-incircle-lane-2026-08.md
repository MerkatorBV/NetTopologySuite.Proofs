# Mesh in-circle lane — JTS #1212 differential (session 1)

**Date**: 2026-08-05.  
**topic:** `mesh` · epic **#68**  
**Upstream tip**: [locationtech/jts#1212](https://github.com/locationtech/jts/pull/1212)
  *Improve triangulation robustness* (micycle1) — supersedes #1094 / #311.  
**Scope**: Extract adversarial / pin vectors; gate against Rocq `INCIRCLE_SIGN`
  (`b64_inCircle` = Phase 5 FFI `nts_rocq_in_circle`); mirror Stage A filter
  from the PR; no new Rocq theorems this session.

**Verdict**: **GREEN** — all corpus EXPECTED pins match oracle; no
Stage-A-**CERTAIN** vs oracle sign conflicts. Stage A correctly declines on
exact zeros and one near-cocircular open case (DD fallback path exercised).

---

## §1 — What #1212 changes

`TrianglePredicate.isInCircleRobust`:

1. Evaluate the Shewchuk in-circle determinant in **double** (coords translated
   so `P` is the origin — same matrix as corpus `inCircle_R`).
2. Stage A error bound  
   `iccerrboundA = (10 + 96·ε)·ε`, `ε = 2⁻⁵³`  
   scaled by the matrix permanent; if `|disc| > errBound`, return `disc > 0`.
3. Else fall back to existing **`isInCircleDDNormalized`** (DD extended precision).

Also: `Vertex.isCCW` → `Orientation.index` (robust CCW).

Claims to close/supersede: JTS #20, #298, #310, #1190 (#1138), GEOS #1286, PR #311.

---

## §2 — Corpus ground truth

| Asset | Role |
|---|---|
| `ArcOrient.inCircle_R` | Exact-reals Shewchuk determinant (Qed algebra) |
| `b64_inCircle` / `INCIRCLE_SIGN` | Extracted binary64 evaluator (oracle / FFI) |
| `DelaunayDegeneratePins` (68-c) | Vertex ZERO + cocircular square ZERO + knife ± |
| `DelaunayLocallyDelaunay` (68-b) | Flip witness POS 3/2 |
| `DelaunayEdgeEmptyCircle` (68-a) | Outside-disk NEG witness |

Sign convention (aligned with JTS “inside iff true” under CCW triangle):

> `POS` ⇔ `(A,B,C)` CCW and `P` strictly inside the circumcircle.

---

## §3 — Deliverables

| Path | Purpose |
|---|---|
| `oracle/gen_jts1212_incircle_vectors.py` | Generator + Stage A mirror + oracle gate |
| `oracle/jts1212_incircle_vectors.txt` | 29-vector table (regenerated by the script) |

```text
python oracle/gen_jts1212_incircle_vectors.py
# or:  --wsl-oracle /path/to/oracle_bin   /   ORACLE_BIN=...
```

---

## §4 — Gate results (2026-08-05)

| Metric | Value |
|---|---|
| Vectors | **29** |
| Oracle pin (flip witness) | **POS 1.5** ✓ |
| EXPECTED pin failures | **0** |
| Stage A CERTAIN vs oracle sign conflicts | **0** |
| Stage A UNCERTAIN | **11** (correct declines) |

### Pin class (EXPECTED set)

| Class | Oracle | Stage A |
|---|---|---|
| Vertex-on-circle (1190 / pin_1190) | ZERO | ZERO · UNCERTAIN |
| Cocircular square diagonals (1039) | ZERO | ZERO · UNCERTAIN |
| Knife in / out | POS / NEG | CERTAIN match |
| Flip witness 68-b | POS | CERTAIN match |
| Outside ABC 68-a | NEG | CERTAIN match |
| Scaled flip (homothety) | POS | CERTAIN match |

**Reading UNCERTAIN on exact ZERO:** Stage A bound is positive whenever
`permanent > 0`, so `|disc| = 0` never clears the bound. The PR **must** fall
through to DD on cocircular / vertex cases — that is the correct behaviour
(cocircular tie: neither flip; vertices: not strictly inside).

### JTS #1212 regression sites

| Vector | Oracle | Stage A | Note |
|---|---|---|---|
| `jts1212_1190_vertex_*` | ZERO | UNCERTAIN | Obtuse constrained triangle sites |
| `jts1212_circle_quad_*` | NEG | CERTAIN | Densified near-circle sample quads |
| `jts1212_circle_cardinal` | ZERO | UNCERTAIN | 4 cardinals nearly cocircular |
| `jts1212_geos1040_quad` | NEG | CERTAIN | Near-cocircular GEOS#1040 |
| **`jts1212_geos955_subset`** | **POS** | **ZERO · UNCERTAIN** | **Stage A disc = 0; oracle POS → DD path required** |
| `jts1212_jts20_*` | NEG/POS | CERTAIN / UNCERTAIN | Near-coincident sites from #20 WKB |
| unit-circle ε nudges | POS | CERTAIN | 1e-12 … 1e-6 |

The **GEOS 955 subset** is the load-bearing illustration that double Stage A is
insufficient alone: without the DD fallback, a naive `disc > 0` test would
return **false** (treating zero as outside) while the extracted oracle says
**POS** (strictly inside under b64 evaluation). #1212’s uncertain→DD branch is
exactly the fix shape.

---

## §5 — Implications for JTS / NTS

1. **#1212 filter shape is consistent** with corpus Stage A discipline (same
   `iccerrboundA` constant family as Shewchuk; uncertain on the knife edge).
2. **Do not** treat double `disc` alone as Delaunay ground truth near
   cocircularity — use DD / exact / Rocq `INCIRCLE_SIGN`.
3. Corpus pins remain the **certified** regression set; JTS test sites are
   **open** (no Qed EXPECTED) but oracle-signed for differential use.
4. NTS port of #1212 should re-run this script against the same table once
   `TrianglePredicate` lands in NTS.

---

## §6 — Next sessions (optional)

| Step | Work | Size |
|---|---|---|
| 1b | Add `jts1212_incircle_vectors.txt` to CI smoke (`make -C oracle` optional target) | small |
| 1c | Comment on jts#1212 with GEOS955 StageA=0 / oracle=POS table (numbers only) | 15 min |
| 1d | Extract more quads from full circle expected MULTILINESTRING if needed | medium |
| 2 | Orientation lane (#1093 Ozaki) — separate session | **GREEN** ([jts-1093-orient-lane-2026-08.md](jts-1093-orient-lane-2026-08.md)) |

---

## §7 — Non-claims

- No proof that JTS DD fallback equals `b64_inCircle` bit-for-bit.
- No full re-run of JTS unit tests under Maven.
- No NTS port in this session.

**AI assistance**: Grok (grok-4.5).  
**License**: project documentation (BSD-3-Clause).
